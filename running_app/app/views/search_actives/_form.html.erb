<%= form_with(model: search_active, local: true) do |form| %>
  <% if search_active.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_active.errors.count, "error") %> prohibited this search_active from being saved:</h2>

      <ul>
        <% search_active.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :raceType %>
    <%= form.text_field :raceType %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
